/**
 * Domain classes (classes metiers) used for mapping to datasource and to
 * produce the JSON output for RESTful services.
 * Classes contain JPA and lombok annotation
 */
package com.ensa.absence.domain.entity;